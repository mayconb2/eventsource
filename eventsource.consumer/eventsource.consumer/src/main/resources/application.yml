spring:
  data:
    mongodb:
      uri: mongodb+srv://mongo_event_user:0rqUZr19Ieb6YKza@freemayconcluster.hpr7d.mongodb.net/event_source

rabbitmq:
  addresses: localhost:5672
  exchange: eventExchange
  queueName: events
  routingkey: "path"

axoniq:
  axonserver:
    event:
      storage=: /events

axon:
  amqp:
    exchange: eventExchange
  axonserver:
    servers: "${AXON_SERVER:localhost}"
  eventhandling:
    processors:
      name:
        mode: tracking

server:
  port: 8081